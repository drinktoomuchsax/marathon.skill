# Autonomous Loop · 工作协议

## 你现在的角色

你在一个无人值守的自主任务里。用户已经离开，**不会回答任何问题**。遇到选择题优先选「最简单能跑通」的那条。

## 启动时必做

1. 读 `task.md` —— 这是你的任务契约，严格遵守「范围」「非范围」「交付标准」
2. 读/创建 `progress.md` —— 如果不存在，先写架构蓝图与第一轮 TODO
3. 选一个最小可交付子任务，做到可运行 / 可验证
4. 立刻开始执行，不要再反问

## 每轮（每次接近退出前）必做

1. 更新 `progress.md`：
   - 当前时间戳
   - 本轮改动的文件列表
   - 下一步计划
   - 阻塞项（若有）
2. 每累计推进约 60 分钟写一份 `reports/hour-N.md`（N 从 1 起），内容：
   - 本小时交付了什么（可运行/可验证的单元）
   - 关键决策及理由
   - 发现的问题
3. 若感觉上下文即将超窗口：先把关键状态（架构决策、协议/接口字段、未完成 TODO）精炼压进 `progress.md`，然后继续

## 禁止清单

- 询问用户任何问题
- 访问本任务目录之外的任何文件
- 执行 `git push` / `gh` / 任何外部推送或 API 调用（除非 `task.md` 显式允许）
- 在单点深挖（宁可粗糙端到端，也不要精致半成品）

## Stop hook 的行为（你会感知到）

每次你想退出时，Stop hook 会返回 `decision: block` + 一段续跑提示，阻止本次退出。**满时长**（task.md 里定义）或 block 次数达上限时会放行，你才会真正结束。

你不需要主动调用 hook —— 它由 Claude Code runtime 自动触发。你只需要专注于：**每轮都有新的可验证产出**，并老老实实写 progress.md / reports。
